// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/hofchurchng/church-backend/internal/v2/ent/consumerreceipt"
)

// ConsumerReceipt is the model entity for the ConsumerReceipt schema.
type ConsumerReceipt struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Version holds the value of the "version" field.
	Version int `json:"version,omitempty"`
	// ConsumerName holds the value of the "consumer_name" field.
	ConsumerName string `json:"consumer_name,omitempty"`
	// EventID holds the value of the "event_id" field.
	EventID uuid.UUID `json:"event_id,omitempty"`
	// ProcessedAt holds the value of the "processed_at" field.
	ProcessedAt  time.Time `json:"processed_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ConsumerReceipt) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case consumerreceipt.FieldVersion:
			values[i] = new(sql.NullInt64)
		case consumerreceipt.FieldConsumerName:
			values[i] = new(sql.NullString)
		case consumerreceipt.FieldProcessedAt:
			values[i] = new(sql.NullTime)
		case consumerreceipt.FieldID, consumerreceipt.FieldEventID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ConsumerReceipt fields.
func (_m *ConsumerReceipt) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case consumerreceipt.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case consumerreceipt.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				_m.Version = int(value.Int64)
			}
		case consumerreceipt.FieldConsumerName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field consumer_name", values[i])
			} else if value.Valid {
				_m.ConsumerName = value.String
			}
		case consumerreceipt.FieldEventID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field event_id", values[i])
			} else if value != nil {
				_m.EventID = *value
			}
		case consumerreceipt.FieldProcessedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field processed_at", values[i])
			} else if value.Valid {
				_m.ProcessedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ConsumerReceipt.
// This includes values selected through modifiers, order, etc.
func (_m *ConsumerReceipt) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this ConsumerReceipt.
// Note that you need to call ConsumerReceipt.Unwrap() before calling this method if this ConsumerReceipt
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ConsumerReceipt) Update() *ConsumerReceiptUpdateOne {
	return NewConsumerReceiptClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ConsumerReceipt entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ConsumerReceipt) Unwrap() *ConsumerReceipt {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ConsumerReceipt is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ConsumerReceipt) String() string {
	var builder strings.Builder
	builder.WriteString("ConsumerReceipt(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", _m.Version))
	builder.WriteString(", ")
	builder.WriteString("consumer_name=")
	builder.WriteString(_m.ConsumerName)
	builder.WriteString(", ")
	builder.WriteString("event_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.EventID))
	builder.WriteString(", ")
	builder.WriteString("processed_at=")
	builder.WriteString(_m.ProcessedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ConsumerReceipts is a parsable slice of ConsumerReceipt.
type ConsumerReceipts []*ConsumerReceipt
