// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/hofchurchng/church-backend/internal/v2/ent/delegation"
	"github.com/hofchurchng/church-backend/internal/v2/ent/scopedgrant"
)

// Delegation is the model entity for the Delegation schema.
type Delegation struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Version holds the value of the "version" field.
	Version int `json:"version,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// ParentGrantID holds the value of the "parent_grant_id" field.
	ParentGrantID uuid.UUID `json:"parent_grant_id,omitempty"`
	// DelegatedToAssignmentID holds the value of the "delegated_to_assignment_id" field.
	DelegatedToAssignmentID uuid.UUID `json:"delegated_to_assignment_id,omitempty"`
	// ValidFrom holds the value of the "valid_from" field.
	ValidFrom time.Time `json:"valid_from,omitempty"`
	// ValidUntil holds the value of the "valid_until" field.
	ValidUntil time.Time `json:"valid_until,omitempty"`
	// RevokedAt holds the value of the "revoked_at" field.
	RevokedAt *time.Time `json:"revoked_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DelegationQuery when eager-loading is set.
	Edges        DelegationEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DelegationEdges holds the relations/edges for other nodes in the graph.
type DelegationEdges struct {
	// ParentGrant holds the value of the parent_grant edge.
	ParentGrant *ScopedGrant `json:"parent_grant,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ParentGrantOrErr returns the ParentGrant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DelegationEdges) ParentGrantOrErr() (*ScopedGrant, error) {
	if e.ParentGrant != nil {
		return e.ParentGrant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: scopedgrant.Label}
	}
	return nil, &NotLoadedError{edge: "parent_grant"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Delegation) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case delegation.FieldVersion:
			values[i] = new(sql.NullInt64)
		case delegation.FieldUpdatedAt, delegation.FieldValidFrom, delegation.FieldValidUntil, delegation.FieldRevokedAt, delegation.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case delegation.FieldID, delegation.FieldParentGrantID, delegation.FieldDelegatedToAssignmentID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Delegation fields.
func (_m *Delegation) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case delegation.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case delegation.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				_m.Version = int(value.Int64)
			}
		case delegation.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case delegation.FieldParentGrantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field parent_grant_id", values[i])
			} else if value != nil {
				_m.ParentGrantID = *value
			}
		case delegation.FieldDelegatedToAssignmentID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field delegated_to_assignment_id", values[i])
			} else if value != nil {
				_m.DelegatedToAssignmentID = *value
			}
		case delegation.FieldValidFrom:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field valid_from", values[i])
			} else if value.Valid {
				_m.ValidFrom = value.Time
			}
		case delegation.FieldValidUntil:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field valid_until", values[i])
			} else if value.Valid {
				_m.ValidUntil = value.Time
			}
		case delegation.FieldRevokedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field revoked_at", values[i])
			} else if value.Valid {
				_m.RevokedAt = new(time.Time)
				*_m.RevokedAt = value.Time
			}
		case delegation.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Delegation.
// This includes values selected through modifiers, order, etc.
func (_m *Delegation) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryParentGrant queries the "parent_grant" edge of the Delegation entity.
func (_m *Delegation) QueryParentGrant() *ScopedGrantQuery {
	return NewDelegationClient(_m.config).QueryParentGrant(_m)
}

// Update returns a builder for updating this Delegation.
// Note that you need to call Delegation.Unwrap() before calling this method if this Delegation
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Delegation) Update() *DelegationUpdateOne {
	return NewDelegationClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Delegation entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Delegation) Unwrap() *Delegation {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Delegation is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Delegation) String() string {
	var builder strings.Builder
	builder.WriteString("Delegation(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", _m.Version))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("parent_grant_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ParentGrantID))
	builder.WriteString(", ")
	builder.WriteString("delegated_to_assignment_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.DelegatedToAssignmentID))
	builder.WriteString(", ")
	builder.WriteString("valid_from=")
	builder.WriteString(_m.ValidFrom.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("valid_until=")
	builder.WriteString(_m.ValidUntil.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.RevokedAt; v != nil {
		builder.WriteString("revoked_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Delegations is a parsable slice of Delegation.
type Delegations []*Delegation
